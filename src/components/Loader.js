function Loader() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#0f172a",
        color: "white",
        fontSize: "1.5rem",
      }}
    >
      ✈️ Loading WanderNest...
    </div>
  );
}

export default Loader;